Open Package Manager Console and run the below commands to add migrations

01. Run "add-migration initdata" in the NuGet package manager console
02. Run "update-database" in the NuGet package manager console
